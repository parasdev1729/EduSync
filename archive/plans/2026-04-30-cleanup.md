# Project Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Clean up the project by archiving utility scripts and deleting unnecessary folders.

**Architecture:** Moving scripts to a root `archive/` directory and permanently deleting `docs/` and `server/tests/`.

**Tech Stack:** Bash commands for file manipulation.

---

### Task 1: Archive Setup

**Files:**
- Create: `archive/server-seed/`

- [ ] **Step 1: Create the archive directory structure**

Run: `mkdir -p archive/server-seed archive/plans`
Expected: Directories created.

- [ ] **Step 2: Commit initial archive structure**

```bash
git add archive/
git commit -m "chore: setup archive directory"
```

---

### Task 2: Move Utility Scripts

**Files:**
- Move: `seed_admin.js` -> `archive/`
- Move: `server/list_users.js` -> `archive/`
- Move: `server/set_passwords.js` -> `archive/`
- Move: `server/seed/` contents -> `archive/server-seed/`

- [ ] **Step 1: Move root seeding script**

Run: `mv seed_admin.js archive/`
Expected: File moved.

- [ ] **Step 2: Move server utility scripts**

Run: `mv server/list_users.js archive/ && mv server/set_passwords.js archive/`
Expected: Files moved.

- [ ] **Step 3: Move server seed contents**

Run: `mv server/seed/* archive/server-seed/ && rmdir server/seed`
Expected: Files moved and empty directory removed.

- [ ] **Step 4: Commit moved scripts**

```bash
git add .
git commit -m "chore: move utility and seeding scripts to archive"
```

---

### Task 3: Permanent Removal

**Files:**
- Delete: `docs/`
- Delete: `server/tests/`

- [ ] **Step 1: Remove docs folder**

Run: `rm -rf docs/`
Expected: Folder removed.

- [ ] **Step 2: Remove server tests folder**

Run: `rm -rf server/tests/`
Expected: Folder removed.

- [ ] **Step 3: Commit removals**

```bash
git add .
git commit -m "chore: remove unnecessary docs and tests"
```

---

### Task 4: Final Verification

- [ ] **Step 1: Verify directory structure**

Run: `ls -R archive/ && ls docs server/tests server/seed`
Expected: `archive/` contains all moved files. `docs`, `server/tests`, and `server/seed` do not exist.

- [ ] **Step 2: Move this plan to archive/plans**

Run: `mv archive/plans/2026-04-30-cleanup.md archive/plans/` (Self-reference if needed, but I'll write it there directly)
